module.exports = {
  password: "fk_56xz",
  
  // Dont use or we will have to change the password and
  // make the project not open source and the github repo.
  //-------------------------------------------------------
  // No usar o tendremos que cambar la contraseña y hacer
  // que el proyecto no sea open source ademas de cerrar el
  // repositorio de github.

  host: "localhost",
  user: "root",
  database: "sys"
}
